-- ============================================================
-- 移除 分配角色 和 分配用户 菜单
-- 这两个是用户管理和角色管理的内嵌子页面，无实际业务价值
-- ============================================================

-- 1. 删除角色-菜单关联
DELETE FROM sys_role_menu WHERE menu_id IN (130, 131);

-- 2. 删除菜单项
DELETE FROM sys_menu WHERE menu_id IN (130, 131);
